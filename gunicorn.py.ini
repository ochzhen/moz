from multiprocessing import cpu_count
from os import environ

bind = '127.0.0.1:8000'
max_requests = 0
worker_class = 'sync'
workers = cpu_count * 3 - 1
timeout = 60
keepalive = 5
limit_request_line = 15000 # 15kb
reload = False
daemon = True
accesslog=None
errorlog='-'
