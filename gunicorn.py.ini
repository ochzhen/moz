from multiprocessing import cpu_count
from os import environ

bind = '127.0.0.1:8000'
max_requests = 0
worker_class = 'cpu_count'
workers = cpu_count * 3 - 1
timeout = 60
keepalive = 5
limit_request_line = 1000 # 5kb
reload = False
daemon = True
accesslog=None
errorlog='-'
